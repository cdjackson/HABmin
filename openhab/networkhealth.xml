<!-- Binding configuration for the Network Health binding in openHAB -->
<binding>
    <!-- Full name presented to the user -->
    <name>Network Health Binding</name>
    <!-- PID name used within the OSGi layer -->
    <pid>networkhealth</pid>
    <!-- Binding type: protocol, ui -->
    <type>protocol</type>
    <!-- Author -->
    <author>Thomas Eichstaedt-Engelen</author>
    <!-- Current version of the binding to which the file is applicable -->
    <version>1.3.0</version>
    <!-- Minimum version of openHAB required to run this binding -->
    <oh_version>1.3.0</oh_version>

    <!-- Binding configuration settings -->
    <binding.config>
        <config.setting>
            <name>timeout</name>
            <label>Ping Timeout</label>
            <description>The ping timeout in milliseconds.</description>
            <optional>true</optional>
            <defaultvalue>5000</defaultvalue>
            <minimum></minimum>
            <maximum></maximum>
            <values></values>
        </config.setting>
        <config.setting>
            <name>refresh</name>
            <label>Refresh Interval</label>
            <description>The binding refresh inverval in milliseconds.</description>
            <optional>true</optional>
            <defaultvalue>60000</defaultvalue>
            <minimum></minimum>
            <maximum></maximum>
            <values></values>
        </config.setting>
    </binding.config>

    <!-- Item binding configuration -->
    <binding.items>
        <binding.io>
        </binding.io>
    </binding.items>
</binding>





